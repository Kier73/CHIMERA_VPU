#ifndef HAL_IKERNEL_H
#define HAL_IKERNEL_H

class IKernel {
public:
    virtual ~IKernel() = default; // Important for base classes with virtual functions
    // Add pure virtual functions later as needed
};

#endif // HAL_IKERNEL_H
